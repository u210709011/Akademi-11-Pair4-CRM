# Yerel gelistirici makinesi (varsayilan ortam)
# front-end: ng serve  -> 4200
# back-end : api-gateway -> 8080
base.url=http://localhost:4200
api.base.url=http://localhost:8080
# Keycloak, infra/compose.yml icinde 8180 portunda calisir (start-dev --http-port=8180)
keycloak.url=http://localhost:8180

# Dokumandaki demo kullanici (back-end/infra/keycloak/crm-realm.json).
# CI/paylasimli ortamlarda CRM_UI_USERNAME / CRM_UI_PASSWORD ortam degiskenleri kullanilmalidir.
ui.username=salesperson
ui.password=password
