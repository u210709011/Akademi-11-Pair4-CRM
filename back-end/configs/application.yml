# Tum servisler ve tum ortamlar icin ortak konfigurasyon.
# Ortama ozel degerler: application-dev.yml, application-prod.yml
# Servise ozel dosyalar: configs/<servis-adi>/<servis-adi>[-<profil>].yml

eureka:
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
