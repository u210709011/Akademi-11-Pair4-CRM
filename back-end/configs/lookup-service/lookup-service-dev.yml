# Lookup Service - dev ortami: tum altyapi lokalde (podman compose) calisir.

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

keycloak:
  issuer-uri: http://localhost:8180/realms/crm

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lookup_db
    username: crm
    password: crm

logging:
  level:
    com.etiya.crm.lookupservice: DEBUG
