# Dev ortami: tum altyapi lokalde (podman compose) calisir.

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

keycloak:
  issuer-uri: http://localhost:8180/realms/crm

spring:
  kafka:
    bootstrap-servers: localhost:9092
  data:
    redis:
      host: localhost
      port: 6379
